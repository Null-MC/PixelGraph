<Window x:Class="PixelGraph.UI.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:PixelGraph.UI.ViewModels"
        xmlns:controls="clr-namespace:PixelGraph.UI.Controls"
        d:DataContext="{d:DesignInstance vm:MainWindowDVM, IsDesignTimeCreatable=True}"
        Title="PixelGraph" Height="600" Width="1000" mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top">
            <StackPanel.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#222"/>
                    <GradientStop Color="Black" Offset="1"/>
                </LinearGradientBrush>
            </StackPanel.Background>
            <DockPanel Margin="4,6,4,4" LastChildFill="False">
                <Button Name="NewPackButton" DockPanel.Dock="Left" Margin="4" ToolTip="create a new pack profile" Click="NewPackButton_OnClick">
                    <StackPanel Orientation="Horizontal">
                        <md:PackIcon Kind="FolderOpen" />
                        <TextBlock Text="New" VerticalAlignment="Center" Margin="4,0,0,0" />
                    </StackPanel>
                </Button>
                <Button Name="OpenPackButton" DockPanel.Dock="Left" Margin="4" ToolTip="open an existing pack profile" Click="OpenPackButton_OnClick">
                    <StackPanel Orientation="Horizontal">
                        <md:PackIcon Kind="FolderOpen" />
                        <TextBlock Text="Open" VerticalAlignment="Center" Margin="4,0,0,0" />
                    </StackPanel>
                </Button>
                <Button Name="PublishPackButton" DockPanel.Dock="Right" Margin="4" ToolTip="publish current pack profile" Click="PublishPackButton_OnClick">
                    <StackPanel Orientation="Horizontal">
                        <md:PackIcon Kind="FileExport" />
                        <TextBlock Text="Publish" VerticalAlignment="Center" Margin="4,0,0,0" />
                    </StackPanel>
                </Button>
            </DockPanel>
        </StackPanel>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBar.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black"/>
                    <GradientStop Color="#111" Offset="1"/>
                </LinearGradientBrush>
            </StatusBar.Background>
            <StatusBarItem>
                <TextBlock Name="PackFilenameLabel" Text="{Binding PackFilename, Mode=OneWay}" Foreground="{DynamicResource SecondaryHueDarkBrush}"/>
            </StatusBarItem>
            <!--<StatusBarItem>
                <ProgressBar Name="StatusProgress" Width="90" />
            </StatusBarItem>-->
        </StatusBar>
        <TabControl Style="{StaticResource MaterialDesignTabControl}" Visibility="{Binding EditVisibility, Mode=OneWay}">
            <TabItem Header="Pack">
                <Grid MinWidth="320" MaxWidth="600" Margin="12,24,12,12" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Column="0" Grid.Row="0" Margin="4" VerticalAlignment="Center" Text="Game Edition" FontSize="14" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <ComboBox Grid.Column="1" Grid.Row="0" SelectedValuePath="Content" SelectedValue="{Binding GameEdition}" Margin="4" TextBlock.FontSize="14" Foreground="{DynamicResource SecondaryHueMidBrush}" HorizontalContentAlignment="Center">
                        <ComboBoxItem Content="Java"/>
                        <ComboBoxItem Content="Bedrock"/>
                    </ComboBox>
                    <TextBlock Grid.Column="2" Grid.Row="0" VerticalAlignment="Center" Text="Pack Version" FontSize="14" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="4,4,4,4" HorizontalAlignment="Right"/>
                    <TextBox Grid.Column="3" Grid.Row="0" Margin="4" Text="{Binding PackFormat}" Foreground="{DynamicResource SecondaryHueMidBrush}" HorizontalContentAlignment="Center" Width="64" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Column="0" Grid.Row="1" Margin="4" VerticalAlignment="Center" Text="Description" FontSize="14" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <TextBox Grid.Column="1" Grid.Row="1" Margin="4" Grid.ColumnSpan="3" Text="{Binding PackDescription}" Foreground="{DynamicResource SecondaryHueMidBrush}"/>
                    <TextBlock Grid.Column="0" Grid.Row="2" Margin="4" VerticalAlignment="Center" Text="Tags" FontSize="14" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <TextBox Grid.Column="1" Grid.Row="2" Margin="4" Grid.ColumnSpan="3" Text="{Binding PackTags}" Foreground="{DynamicResource SecondaryHueMidBrush}"/>
                </Grid>
            </TabItem>
            <TabItem Header="Encoding">
                <StackPanel Orientation="Vertical" Margin="6,24,6,6">
                    <Grid MinWidth="320" MaxWidth="600" Margin="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Left" Margin="8,0" Text="Input Format" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center"/>
                            <ComboBox DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding InputFormat}" Foreground="{DynamicResource SecondaryHueMidBrush}" MaxWidth="140" HorizontalContentAlignment="Center">
                                <ComboBox.ItemsSource>
                                    <vm:EncodingFormatValues/>
                                </ComboBox.ItemsSource>
                            </ComboBox>
                        </DockPanel>
                        <DockPanel Grid.Column="1">
                            <TextBlock DockPanel.Dock="Left" Margin="8,0" Text="Output Format" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center"/>
                            <ComboBox DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding OutputFormat}" Foreground="{DynamicResource SecondaryHueMidBrush}" MaxWidth="140" HorizontalContentAlignment="Center">
                                <ComboBox.ItemsSource>
                                    <vm:EncodingFormatValues/>
                                </ComboBox.ItemsSource>
                            </ComboBox>
                        </DockPanel>
                    </Grid>
                    <Grid MinWidth="320" MaxWidth="600" Margin="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.Resources>
                            <Style TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignComboBox}">
                                <Setter Property="SelectedValuePath" Value="Key"/>
                                <Setter Property="DisplayMemberPath" Value="Value"/>
                                <Setter Property="Margin" Value="4"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="{DynamicResource SecondaryHueMidBrush}"/>
                                <Setter Property="ItemsSource">
                                    <Setter.Value>
                                        <vm:EncodingChannelValues/>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Grid.Resources>
                        <Border Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="4" CornerRadius="8,8,0,0" BorderBrush="Black" BorderThickness="1">
                            <Border.Background>
                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                    <GradientStop Color="#FF494949"/>
                                    <GradientStop Color="Black" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        <Border Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="4" Grid.RowSpan="5" Background="#FF232323"/>
                        <ComboBox Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding EncodingTag}" Foreground="{DynamicResource SecondaryHueMidBrush}" HorizontalContentAlignment="Center">
                            <ComboBox.ItemsSource>
                                <vm:TextureTagValues/>
                            </ComboBox.ItemsSource>
                        </ComboBox>
                        <TextBlock Grid.Column="2" Grid.Row="0" Margin="6" Text="Input" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="3" Grid.Row="0" Margin="6" Text="Output" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Grid.Row="1" Margin="6,2" Orientation="Horizontal" HorizontalAlignment="Left">
                            <md:PackIcon Kind="Circle" VerticalAlignment="Center" Foreground="Red"/>
                            <TextBlock Margin="4,0" Text="Red" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                        </StackPanel>
                        <controls:ComboBoxEx Grid.Column="2" Grid.Row="1" SelectedValue="{Binding InputRed}"/>
                        <controls:ComboBoxEx Grid.Column="3" Grid.Row="1" SelectedValue="{Binding OutputRed}"/>
                        <StackPanel Grid.Column="1" Grid.Row="2" Margin="6,2" Orientation="Horizontal" HorizontalAlignment="Left">
                            <md:PackIcon Kind="Circle" VerticalAlignment="Center" Foreground="Green"/>
                            <TextBlock Margin="4,0" Text="Green" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <controls:ComboBoxEx Grid.Column="2" Grid.Row="2" SelectedValue="{Binding InputGreen}"/>
                        <controls:ComboBoxEx Grid.Column="3" Grid.Row="2" SelectedValue="{Binding OutputGreen}"/>
                        <StackPanel Grid.Column="1" Grid.Row="3" Margin="6,2" Orientation="Horizontal" HorizontalAlignment="Left">
                            <md:PackIcon Kind="Circle" VerticalAlignment="Center" Foreground="Blue"/>
                            <TextBlock Margin="4,0" Text="Blue" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <controls:ComboBoxEx Grid.Column="2" Grid.Row="3" SelectedValue="{Binding InputBlue}"/>
                        <controls:ComboBoxEx Grid.Column="3" Grid.Row="3" SelectedValue="{Binding OutputBlue}"/>
                        <StackPanel Grid.Column="1" Grid.Row="4" Margin="6,2" Orientation="Horizontal" HorizontalAlignment="Left">
                            <md:PackIcon Kind="CircleOutline" VerticalAlignment="Center" Foreground="White"/>
                            <TextBlock Margin="4,0" Text="Alpha" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <controls:ComboBoxEx Grid.Column="2" Grid.Row="4" SelectedValue="{Binding InputAlpha}"/>
                        <controls:ComboBoxEx Grid.Column="3" Grid.Row="4" SelectedValue="{Binding OutputAlpha}"/>
                        <CheckBox Grid.Column="3" Grid.Row="5" Content="Export Texture" HorizontalAlignment="Center" Margin="8" IsChecked="{Binding ExportTexture}" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    </Grid>
                </StackPanel>
            </TabItem>
            <TabItem Header="Content">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="3"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TreeView Grid.Column="0" Background="#FF1B1B1B"/>
                    <GridSplitter Grid.Column="1" Background="White" ResizeBehavior="PreviousAndNext" ResizeDirection="Columns" ShowsPreview="True" HorizontalAlignment="Stretch"/>
                    <TextBlock Grid.Column="2" Text="Stuff goes here..." HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
